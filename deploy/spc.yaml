apiVersion: openebs.io/v1alpha1
kind: StoragePoolClaim
metadata:
  name: cstor-sparse-pool
spec:
  maxPools: 1
  name: cstor-sparse-pool
  poolSpec:
    cacheFile: ""
    overProvisioning: false
    poolType: striped
  type: sparse
